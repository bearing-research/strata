\* Both fixes (3.13+ or a re-notify in ResizableLimiter, and eviction of idle limiters only): all hold.
CONSTANTS
    Requests = {r1, r2, r3, r4}
    Capacity = 2
    MaxGen = 3
    NotifyOnCancel = TRUE
    SafeEviction = TRUE
SPECIFICATION Spec
INVARIANTS TypeOK NoLostWakeup WithinQuota DrainSeesAll
