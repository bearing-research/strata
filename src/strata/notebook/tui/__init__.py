"""Strata Notebook TUI — a read-only Textual spectator for a live session.

Attaches to a running notebook session over the WS protocol and renders its
cells + live execution stream, optimized for watching an agent drive a notebook
from another terminal. Phase 1 of #37 (issue #310). Install via the ``[tui]``
extra; launch with ``strata-notebook-tui``.
"""
